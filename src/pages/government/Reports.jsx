import { motion } from 'framer-motion'
import Card from './_Card'
const reports=['Daily procurement report','Mandi waiting-time report','MSP compliance report','Farmer transaction summary']
export default function Reports({onGenerate}){return <motion.div initial={{opacity:0}} animate={{opacity:1}}><div className="page-heading"><div><small>OFFICIAL REPORTING</small><h1>Reports & Downloads</h1><p>Generate operational and procurement reports for review.</p></div></div><div className="support-grid">{reports.map((name,i)=><motion.div key={name} initial={{opacity:0,y:18}} animate={{opacity:1,y:0}} transition={{delay:i*.08}}><Card className="report-card"><span className="support-icon">▤</span><h3 style={{marginTop:14}}>{name}</h3><p>PDF-ready summary for the selected reporting period.</p><button className="secondary small" onClick={()=>onGenerate(name)}>Generate Report →</button></Card></motion.div>)}</div></motion.div>}
