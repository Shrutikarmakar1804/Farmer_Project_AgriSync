import { motion } from 'framer-motion'
import Card from './_Card'
export default function Payments({quantity,onToast}){const amount=quantity*2585;return <motion.div initial={{opacity:0,scale:.98}} animate={{opacity:1,scale:1}}><div className="page-heading"><div><small>PAYMENT STATUS</small><h1>Procurement Payment</h1><p>Review the transaction value before issuing the farmer receipt.</p></div><span className="pill success">Payment Completed</span></div><Card className="payment-card"><span className="eyebrow">TRANSACTION TK-1082</span><h2 style={{fontFamily:'Space Grotesk',fontSize:30}}>₹{amount.toLocaleString('en-IN')}</h2><div className="payment-grid"><div><span>Net weight</span><b>{quantity.toFixed(1)} quintals</b></div><div><span>MSP rate</span><b>₹2,585 / q</b></div><div><span>Status</span><b>Settled</b></div></div><button className="primary" onClick={()=>onToast('Receipt prepared in demo mode')}>Download Receipt ↓</button></Card></motion.div>}
