import { motion } from 'framer-motion'
import Card from './_Card'
export default function Weighbridge({gross,tare,setGross,setTare,onToast,onProceed}){const net=Math.max(0,Number(gross||0)-Number(tare||0)),q=net/100;return <motion.div initial={{opacity:0,y:16}} animate={{opacity:1,y:0}}><div className="page-heading"><div><small>SCALE LANE 02 • ACTIVE</small><h1>Weighbridge</h1><p>Record gross and tare weights to certify net produce.</p></div><span className="pill success">Calibrated & verified</span></div><Card><div className="form-grid"><div className="field"><label>Gross weight (kg)</label><input value={gross} onChange={e=>setGross(e.target.value)} type="number"/></div><div className="field"><label>Tare weight (kg)</label><input value={tare} onChange={e=>setTare(e.target.value)} type="number"/></div><div className="field"><label>Vehicle</label><input defaultValue="WB-02-X-4321"/></div><div className="field"><label>Bag type</label><select defaultValue="Standard"><option>Standard</option><option>Jute</option><option>HDPE</option></select></div></div><div className="metric-grid" style={{marginTop:18}}><div className="metric"><span>Net weight</span><b>{net.toLocaleString()} kg</b></div><div className="metric"><span>Quintals</span><b>{q.toFixed(1)} q</b></div><div className="metric"><span>Lane</span><b>02</b></div></div><div className="button-row end" style={{marginTop:18}}><button className="secondary" onClick={()=>onToast?.('Weighbridge reading refreshed')}>↻ Recapture</button><button className="primary" onClick={onProceed}>Certify & Continue →</button></div></Card></motion.div>}
