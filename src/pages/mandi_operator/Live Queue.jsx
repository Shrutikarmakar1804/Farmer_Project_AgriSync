import { motion } from 'framer-motion'
import Card from './_Card'
export default function LiveQueue({rows,onAction,onRefresh,onToast}){return <motion.div initial={{opacity:0}} animate={{opacity:1}}><div className="page-heading"><div><small>MANDI CONTROL ROOM • LIVE</small><h1>Live Queue</h1><p>Keep farmer movement smooth and reduce unnecessary waiting.</p></div><button className="primary" onClick={()=>onAction('gate')}>⌗ Scan QR Slot</button></div><div className="operator-stats">{[['24','Farmers today'],['7','In queue'],['18 min','Avg. waiting'],['₹8.4L','Procurement value']].map(([v,l],i)=><motion.div key={l} initial={{opacity:0,y:12}} animate={{opacity:1,y:0}} transition={{delay:i*.07}}><b>{v}</b><span>{l}</span></motion.div>)}</div><div className="operator-layout" style={{display:'grid',gridTemplateColumns:'1fr 280px',gap:16}}><Card><div className="card-heading"><div><h3>Live procurement queue</h3><p>Priority and status update continuously.</p></div><button className="secondary small" onClick={onRefresh}>↻ Refresh</button></div><div className="table-wrap"><table><thead><tr><th>Token</th><th>Farmer</th><th>Crop</th><th>Qty</th><th>Status</th><th>Action</th></tr></thead><tbody>{rows.map((r,i)=><motion.tr key={r[0]} initial={{opacity:0}} animate={{opacity:1}} transition={{delay:i*.06}}><td><b>{r[0]}</b></td><td>{r[1]}</td><td>{r[2]}</td><td>{r[3]}</td><td><span className={`pill ${r[4]==='Verified'?'success':'checking'}`}>{r[4]}</span></td><td><button className="action-btn" onClick={()=>onAction('gate')}>Process →</button></td></motion.tr>)}</tbody></table></div></Card><Card><h3>Quick actions</h3>{[['⌗ Scan QR Code','green',()=>onAction('gate')],['⇄ Override Queue','gold',()=>onToast('Queue override opened')],['⚠ Alert Center','gold',()=>onToast('No critical alerts')]].map(([t,c,f])=><button key={t} className={`quick ${c}`} onClick={f}>{t}<b>→</b></button>)}</Card></div><div style={{marginTop:16}}><Card><h3>Throughput today</h3><div className="bar-chart">{[45,32,55,68,82,72,90,65,52,38,28,20].map((h,i)=><motion.span key={i} style={{height:`${h}%`}} initial={{scaleY:0}} animate={{scaleY:1}} transition={{delay:i*.04}}><i/></motion.span>)}</div></Card></div></motion.div>}
